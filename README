This is the gmarathon http://gmarathon.sourceforge.net/ , ported to Lazarus freepascal 


Required third-party components that will be replaced with lazarus freepascal units 

What i have found for the moment :http://wiki.lazarus.freepascal.org/SynEdit
http://wiki.lazarus.freepascal.org/VirtualTreeview


Se bellow the rest of the components needed :

Here you will find informations about the Delphi packages and components that you need to successfully compile Marathon.
We start with the third party packages, which are not included in CVS. Please download and install them:
The current CVS state of Marathon uses IB Objects 4.3A. If you don't have this version or you never had IB Objects before, just drop me an E-Mail asking for it. Please also CC this E-Mail to Jason Wharton, the author of IB Objects, because else I can't send you the files, due to IB Objects is without cost but not free for Marathon contributors. This means that there is no cost obligation, but you are required to register your usage of IB Objects and agree to the terms of the Trustware License which IB Objects uses.
Synedit 2003-09-29-CVS, for installation instructions please consult the Synedit website or the included documentation.
Toolbar 2000 2.1.2, for installation instructions please consult the Toolbar 2000 website or the included documentation.
rmControls 1.92, for installation instructions please consult the included documentation or visit Ryans homepage at Ryan Mills Homepage.
Virtual TreeView 3.8.3 and for Delphi versions prior to 7 you also need Theme Manager 1.9.15, for installation instructions please consult the Virtual TreeView website or the included documentation.
TPagePrinter 2.0 (with Marathon specific bug fixes), install in Delphi as a component using "PgPrnReg.pas"
TDataSetPrint 1.23 (with Marathon specific bug fixes), install in Delphi as a component using "DSprint.pas"
THexEditor 1.16 (with Marathon specific bug fixes), install in Delphi as a component using "Hexeditor.pas"
The following components and packages are included in CVS module "gmarathon", in the "lib" directory, so you don't need to download them here:
Marathon own package, SyntaxMemoWithStuff2("SyntaxMemoWithStuff2\SyntaxMemoWithStuff2Package.dpk")
Marathon own component, TDBPanelEdit("Other\adbpedit.pas")
Marathon own component, TCloseUpCustomComboBox("Other\CloseUpCombo.pas")
Marathon own component, TDiagramTree("Other\DiagramTree.pas")
Marathon own component, TIBPerformanceMonitor("Other\IBPerformanceMonitor.pas")
Marathon own component, TMarathonColorGrid("Other\NewColorGrd.pas")
Modified FreePascal units("Other\xmlread.pas", "Other\xmlwrite.pas", "Other\dom.pas")

## Porting Progress (2026-04-24)

### UI Refactoring (Complete for core units)
*   **Toolbar2000 Replacement**: Replaced `TTBDock`, `TTBToolbar`, `TTBItem`, and `TTBSubmenuItem` with standard LCL `TPanel`, `TToolBar`, `TToolButton`, and `TMenuItem`.
*   **rmControls Replacement**: 
    *   Replaced `TrmPanel` with `TPanel`.
    *   Replaced `TrmComboBox` with `TComboBox`.
    *   Replaced `TrmTabSet` with `TTabControl`.
    *   Replaced `TrmPathTreeView` with `TTreeView`.
    *   Replaced `TrmKeyBindings` with `TComponent` (base).
*   **VirtualTreeView Replacement**: Replaced `TVirtualStringTree` with standard `TTreeView` in the Global Migrate Wizard.

### Database Migration (SQLDB)
*   **Complete Engine Swap**: Systematically refactored the entire project from **IB Objects (IBO)** to the standard Lazarus **SQLDB** suite.
*   **Class Mapping**: `TIBDatabase` -> `TIBConnection`, `TIBTransaction` -> `TSQLTransaction`, `TIBOQuery`/`TIB_Query`/`TIBSQL` -> `TSQLQuery`, `TIBScript` -> `TSQLScript`.
*   **Property Mapping**: Mapped `.Path`/`.Server` to `.DatabaseName`/`.HostName`, and `.InTransaction`/`.Started` to `.Active`.

### Environment & Compatibility
*   **Unit Fixes**: Added `Printer4Lazarus` dependency and integrated `PrintersDlgs`, `LMessages`, and `LCLIntf`.
*   **Message Constants**: Defined missing `WM_` constants locally to avoid Windows-dependency conflicts.
*   **Search Paths**: Updated `marathon.lpi` to include local `lib` folders and cleaned up obsolete Delphi-only library references.

### Remaining Tasks
*   Surgical fixes for specific property or type mismatches introduced by the component replacements (e.g., `TTabControl` vs `TrmTabSet` API differences).
*   Handling of tri-state checkboxes if needed (previously handled by VirtualTreeView).
*   Verification of low-level database metadata extraction logic under SQLDB.

## Build Success (2026-04-25)

`lazbuild --build-mode=Debug src/Source/marathon.lpi` now compiles and links cleanly on Linux (FPC 3.2.2 / Lazarus 3.0, x86_64).

### What was fixed in this session

**IBO → SQLDB property migration (MarathonProjectCache.pas, editor units)**
*   `Q.IB_Connection/IB_Transaction` → `Q.DataBase/Transaction`
*   `T.IB_Connection/Isolation` → `T.DataBase` (isolation removed)
*   `Q.IB_Transaction.Active/Commit/StartTransaction` → cast to `TSQLTransaction(Q.Transaction).*`
*   `BeginBusy/EndBusy` removed (IBO-only)
*   `TIBConnection.Connect/Disconnect/SQLRole` → `Connected := True/False`, `Role`
*   `FConnection.Characteristics.dbVersion` → stubbed `IsIB5`/`IsIB6` returning `True`
*   `ReadComponent/WriteComponent(TMarathonTree)` → stubbed (TMarathonTree is TObject, not TComponent)
*   `ReadXMLFile(doc, File)` → `ReadXMLFile(doc, FileName)` (filename-string overload)

**Linux / FPC compatibility**
*   `ibase60` → `ibase60dyn` everywhere (`ibase60` statically links `-lgds` which doesn't exist on modern Firebird; `ibase60dyn` loads the client dynamically)
*   `blr_*` Firebird type constants defined locally in Globals.pas (removed `ibase60` from that unit entirely)
*   `FileAgeUTF8` / `ForceDirectoriesUTF8` → `FileAge` / `ForceDirectories`
*   `jpeg` unit → `FPReadJPEG, FPWriteJPEG` (FPC equivalent)
*   `ShlObj`, `gssscript_TLB`, `ShellAPI`, `UserEditor` → `{$IFDEF WINDOWS}`
*   `MutexHandle`, `TBRegLoadPositions/SavePositions`, `HELP_FINDER`, `MessageBeep` → `{$IFDEF WINDOWS}`
*   Windows date-format functions (`GetDateFormat`, `LANG_JAPANESE/CHINESE`, …) → stubbed as empty string
*   `WM_GETMINMAXINFO / TWMGetMinMaxInfo` message handlers → `{$IFDEF WINDOWS}` in all editor forms
*   `gssscript_TLB.pas` — wrapped entire COM file in `{$IFNDEF WINDOWS}…end.{$ENDIF}` stub
*   `MarathonAVI.RES` / `Marathon.RES` → corrected lowercase filenames (case-sensitive Linux fs)

**Globals.pas / SynEdit**
*   Added `SynEditTypes`, `SynGutter` to uses for `eo*` option constants
*   `Gutter.ShowLineNumbers` → `Gutter.LineNumberPart(0).Visible`
*   `SearchEngine`, `KeywordCapitalise`, `NavigatorHyperLinkStyle`, `WordList`, `FindDialog*` → `{$IFNDEF FPC}`

**MarathonMain.pas**
*   `AppHelp` signature: `Data: Longint` → `Data: Int64` (matches Lazarus `THelp` callback)
*   `Window1.Add(TToolButton)` → `Window1.Add(TMenuItem)` (correct LCL API)

**UserEditor.pas / .lfm**
*   `TrmTabSet` → `TTabControl` (in both .pas declaration and .lfm form file)

### Roadmap

- [ ] Runtime smoke-test: launch `./marathon` and verify UI appears
- [ ] Firebird connection test: connect to a local Firebird 3.0 database
- [ ] Fix any runtime errors surfaced by the first UI/connection test
- [ ] Restore `SearchEngine` integration (SynEditSearch) for Find/Replace in editors
- [ ] Restore `WordList` / SQL keyword autocompletion
- [ ] Test metadata browsing (tables, views, SPs, triggers) via SQLDB IBConnection
- [ ] Test DDL extraction (`MetaExtract/DDLExtractor.pas`)
- [ ] Test script execution (`ScriptExecutive.pas`)
- [ ] Package a release build
